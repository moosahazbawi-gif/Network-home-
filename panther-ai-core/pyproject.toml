[project]
name = "panther-ai-core"
version = "0.1.0"
description = "Standalone AI control and execution core for Panther-X2"
requires-python = ">=3.9"
dependencies = ["fastapi>=0.100", "uvicorn[standard]>=0.22", "pydantic>=2"]

[project.scripts]
panther-ai = "panther_ai.api:main"
